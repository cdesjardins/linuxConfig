repo forall -c git tag -a $1 -m "$2"
repo forall -c git push --tags
repo manifest -r -o default.xml.tmp
mv default.xml.tmp .repo/manifests/default.xml
pushd .
cd .repo/manifests
git checkout -b $1 
git commit -a -m "$2"
git push -u origin $1
git checkout default
git branch -D $1
popd
